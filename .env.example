# Public Supabase project URL
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co

# Public anon key (safe for browser use when RLS is enabled)
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Never expose service_role key in frontend code or client env files.
# Keep SUPABASE_SERVICE_ROLE_KEY only on trusted server environments.

# Optional: schedule events table name (default: community_events)
# VITE_SUPABASE_EVENTS_TABLE=community_events

# Optional: synthetic email domain used for loginId-based auth (must be RFC-valid)
# VITE_AUTH_LOGIN_DOMAIN=gmail.com
